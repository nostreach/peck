# peck daemon — Python dependencies
#
# System packages required (Debian/Ubuntu):
#   sudo apt install libavdevice-dev libavfilter-dev libavformat-dev \
#     libavcodec-dev libavutil-dev libswscale-dev libswresample-dev libsrtp2-dev
#
# Install:
#   python3 -m venv venv && source venv/bin/activate
#   pip install -r requirements.txt

aiortc==1.15.0
aiohttp==3.14.1
aioice==0.10.2
coincurve==21.0.0
pyyaml==6.0.3

# Optional: GeoIP support (country/region/ASN rules)
# geoip2==5.3.0
